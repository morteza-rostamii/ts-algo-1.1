
const neetCode = {

  arrayLesson() {
    let result = null;

    /* 
    
    # 1 slot = 1 or 0
    # 1 slot = 1bit
    # 1 byte = 8bits
    
    # 1 integer = 4 bytes =: 4*8=32 bites

    # the way that data is stored on memory for array =: is contiguous

    # in case of int =: 4 bytes
    meaning: [1],     [2],    [3] =: one slot after the other.
            4bytes   4bytes  4bytes

    # read an array index: O(1) const operation

    # push to the end of array: O(1)

    # push to the front: O(n)

    # insert value in middle: O(n)

    #===========================================
    # dynamic array:
    ==

    

    */

    // bubble_sort
    function bubbleSort() {

    }

    console.log(result);
  },

  main() {
    this.arrayLesson();
  },
}

export default neetCode